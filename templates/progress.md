# Progress

The automated step ledger (development/STEPS.md). For its declared routes, run `scripts/next-step.sh` from the project root for the current step and reading. Event lines under "Step history" are append-only and written by the script. A changed declared decision or input basis makes old evidence stale. Unconverted work stays in the existing implementer plan under development/TASKS.md, not in invented events here. Closing every listed step does not establish completion of that other work.

- Playbooks: [the ledger ids this project follows, in order, separated by commas; `scripts/next-step.sh --lint` run inside the engine lists the stepped playbooks]

## Step history
