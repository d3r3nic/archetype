# Frontend scaffold: theme system

Use scaffolding/SCAFFOLD-FRONTEND.md for this route and scaffolding/_preamble.md for shared scaffold guidance.

## Step 2: Theme system
Read: #6; #22; #31; #27 § Design tools; scaffolding/_preamble.md § Convention-mapping rule; project: the brand book and the styling source named in References.md § Design Artifact
Produces: the selected styling source and consumption pattern, derived from the artifact, with every committed scheme and context
Check: run project: typecheck, lint, build; evidence: the selected styling contract applied on a test surface in every committed scheme and the accessibility checks relevant to it
Depends on: scaffold-frontend.1; bootstrap.4.5
Basis: decisions and inputs required

Build:
- Apply the styling decision recorded through the preamble: use the accepted artifact and owner constraints, research current runtime choices where the record is silent, implement only the committed schemes and contexts through the selected interface pattern, and record the source, consumers, reason, and applicable access checks.

**Verify:** the test surface follows the selected styling contract in every committed scheme and context; the project commands pass; observed accessibility checks meet the recorded target; a search finds no values that violate the project's recorded consumption policy.
