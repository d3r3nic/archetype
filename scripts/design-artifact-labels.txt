# The Design Artifact brief the UI References templates carry (convention #27). Read by
# scripts/validate-framework.sh (group 7), which keeps the frontend and mobile templates in
# step with this list. A project's own section is not held to it: scripts/validate-design.sh
# reads only the lines it needs.
#
# One label per line, matched literally. A label never holds a colon or a backslash.
# The mobile template adds "Platform parity".
Primary tool
Direction of truth
Artifact location
Published view
Tokens source
Component catalog
Brand book
Design working files
Sync
Brand decided
Every UI state designed
Update responsibility
Complementary tools
Primary context
Committed contexts
Density
Vocabulary
First task
Return tasks
Session length
Captures
