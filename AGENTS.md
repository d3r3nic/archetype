<!-- archetype-managed-entrypoint -->
# Archetype agent entry point

In project installations this file is centrally managed. Local AI must not edit, delete, or replace it. It will be overwritten by the next framework update. Put project-specific guidance in CLAUDE.md.additions, References.md, and protocols/task-context.md. Propose shared changes in the Archetype source repository. The installed revision is recorded in VERSION-LOG.md.

Read CLAUDE.md for the shared rules, then the project context and relevant protocols it routes to. Explicitly read referenced files; do not assume the client expands imports. These repository instructions remain subject to the active instruction hierarchy and granted permissions.

When authorized to work on Archetype itself, follow the factory's development instructions and record the change there before publishing the framework. Do not bootstrap the framework source as a downstream project.
