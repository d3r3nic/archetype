<!-- archetype-managed-entrypoint -->
# Archetype entry point

The rules for this project are in AGENTS.md, beside this file. Read AGENTS.md in full now, before anything else, then CLAUDE.md.additions if it exists. This file exists because some hosts load only this name; it is centrally managed and replaced by each framework update, so nothing is written here.
